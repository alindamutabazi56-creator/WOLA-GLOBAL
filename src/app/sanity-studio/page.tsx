export default function SanityStudioInfo() {
  return (
    <div className="container-shell py-14">
      <h1 className="text-4xl">Sanity Studio setup</h1>
      <p className="mt-3">Sanity schemas are provided in /sanity/schemas. Configure SANITY_PROJECT_ID and dataset, then mount studio as needed.</p>
    </div>
  );
}
